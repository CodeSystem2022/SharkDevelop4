// Creamos una función asignada a ua constante
const saludar = (nombre) => {  //Función de flechas (usar el acento frances)
    return `Hola ${nombre}`; //esto es igual a 'Saludos'+nombre
};

console.log(saludar("Sharkdevelop"));
